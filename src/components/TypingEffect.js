import React from 'react';
import '../assets/stylesheet2.css'; // Make sure your typing styles are here

const TypingEffect = ({ children }) => (
  <div className="typing">
    {children}
  </div>
);

export default TypingEffect;
